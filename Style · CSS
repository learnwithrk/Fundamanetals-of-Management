/* ============================================================
   Fundamentals of Management — Quiz Site
   Palette: deep study-navy + warm brass accent on warm paper.
   Type: Lora (serif, headings/section numbers) + Inter (sans, body/UI)
   ============================================================ */

:root {
  --navy-950: #101b2d;
  --navy-800: #17294a;
  --navy-700: #1f3462;
  --paper: #f6f3ec;
  --paper-card: #fffdf8;
  --ink: #1b2333;
  --ink-soft: #57607a;
  --ink-faint: #8993a8;
  --brass: #b8863f;
  --brass-dark: #8f6528;
  --brass-tint: #f1e2c4;
  --good: #2f6b4f;
  --good-tint: #e2f0e7;
  --bad: #a4402f;
  --bad-tint: #f6e3de;
  --line: #ddd5c2;
  --line-soft: #e9e4d6;
  --radius: 10px;
  --shadow: 0 1px 2px rgba(16, 27, 45, 0.06), 0 8px 24px -12px rgba(16, 27, 45, 0.18);
}

* { box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  margin: 0;
  font-family: "Inter", system-ui, -apple-system, sans-serif;
  background: var(--paper);
  color: var(--ink);
  line-height: 1.55;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4, .serif {
  font-family: "Lora", Georgia, serif;
  color: var(--navy-950);
  margin: 0 0 0.4em;
  font-weight: 600;
}

a { color: var(--brass-dark); }

.container {
  max-width: 880px;
  margin: 0 auto;
  padding: 0 24px;
}

/* ---------- Top bar ---------- */
.topbar {
  background: var(--navy-950);
  color: #eef1f6;
  border-bottom: 3px solid var(--brass);
}
.topbar .container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 18px;
  padding-bottom: 18px;
  gap: 16px;
  flex-wrap: wrap;
}
.brand {
  display: flex;
  align-items: baseline;
  gap: 10px;
  text-decoration: none;
}
.brand-mark {
  font-family: "Lora", serif;
  font-weight: 700;
  font-size: 1.05rem;
  color: var(--brass);
  letter-spacing: 0.02em;
}
.brand-name {
  font-family: "Lora", serif;
  font-size: 1.1rem;
  color: #f3efe4;
}
.learner-chip {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.92rem;
  color: #cfd6e4;
}
.learner-chip strong { color: #fff; font-weight: 600; }
.learner-chip button {
  background: none;
  border: 1px solid #3a4c6f;
  color: #cfd6e4;
  padding: 5px 10px;
  border-radius: 6px;
  font-size: 0.8rem;
  cursor: pointer;
}
.learner-chip button:hover { border-color: var(--brass); color: #fff; }

/* ---------- Hero ---------- */
.hero {
  background: linear-gradient(180deg, var(--navy-950) 0%, var(--navy-800) 100%);
  color: #eef1f6;
  padding: 56px 0 64px;
}
.hero .eyebrow {
  color: var(--brass);
  font-family: "Lora", serif;
  font-style: italic;
  font-size: 1rem;
  margin-bottom: 10px;
}
.hero h1 {
  color: #fff;
  font-size: clamp(1.8rem, 3.4vw, 2.5rem);
  max-width: 20ch;
}
.hero p {
  color: #c3ccdd;
  max-width: 56ch;
  font-size: 1.02rem;
}

/* ---------- Main content offset ---------- */
main { margin-top: -34px; padding-bottom: 80px; }

/* ---------- Quiz list ---------- */
.quiz-list { display: flex; flex-direction: column; gap: 16px; }

.quiz-card {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 22px 24px;
  display: flex;
  align-items: center;
  gap: 20px;
}
.quiz-card .num {
  font-family: "Lora", serif;
  font-size: 1.6rem;
  color: var(--brass-dark);
  min-width: 64px;
  text-align: center;
  border-right: 1px solid var(--line-soft);
  padding-right: 18px;
}
.quiz-card .body { flex: 1; min-width: 0; }
.quiz-card h3 { margin-bottom: 4px; font-size: 1.15rem; }
.quiz-card .meta { color: var(--ink-soft); font-size: 0.9rem; }
.quiz-card .actions { display: flex; gap: 10px; flex-shrink: 0; }

.empty-note {
  border: 1px dashed var(--line);
  border-radius: var(--radius);
  padding: 24px;
  color: var(--ink-soft);
  text-align: center;
}

/* ---------- Buttons ---------- */
.btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: "Inter", sans-serif;
  font-size: 0.92rem;
  font-weight: 600;
  padding: 10px 18px;
  border-radius: 7px;
  border: 1px solid transparent;
  cursor: pointer;
  text-decoration: none;
  transition: transform 0.05s ease, opacity 0.15s ease;
}
.btn:active { transform: translateY(1px); }
.btn-primary { background: var(--brass); color: #241804; }
.btn-primary:hover { background: var(--brass-dark); color: #fff; }
.btn-ghost { background: transparent; color: var(--navy-800); border-color: var(--line); }
.btn-ghost:hover { border-color: var(--brass); color: var(--brass-dark); }
.btn-full { width: 100%; justify-content: center; }
.btn[disabled] { opacity: 0.45; cursor: not-allowed; }

/* ---------- Login gate ---------- */
.login-card {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 28px;
  max-width: 420px;
  margin: 0 auto;
}
.login-card h2 { font-size: 1.2rem; }
.login-card p { color: var(--ink-soft); font-size: 0.92rem; }
.field { margin: 16px 0; }
.field label { display: block; font-size: 0.85rem; color: var(--ink-soft); margin-bottom: 6px; }
.field input {
  width: 100%;
  padding: 11px 12px;
  border: 1px solid var(--line);
  border-radius: 7px;
  font-size: 1rem;
  font-family: inherit;
  background: #fff;
}
.field input:focus { outline: 2px solid var(--brass); outline-offset: 1px; }

/* ---------- Quiz page ---------- */
.quiz-header {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 22px 24px;
  margin-bottom: 22px;
}
.quiz-header .tag {
  display: inline-block;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--brass-dark);
  background: var(--brass-tint);
  padding: 3px 9px;
  border-radius: 5px;
  margin-bottom: 8px;
}
.progress-track {
  height: 6px;
  background: var(--line-soft);
  border-radius: 4px;
  overflow: hidden;
  margin-top: 14px;
}
.progress-fill {
  height: 100%;
  background: var(--brass);
  width: 0%;
  transition: width 0.2s ease;
}

.q-card {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 20px 22px;
  margin-bottom: 14px;
}
.q-card .q-num {
  font-family: "Lora", serif;
  color: var(--brass-dark);
  font-size: 0.95rem;
  margin-bottom: 6px;
}
.q-card .q-text { font-size: 1.03rem; margin-bottom: 14px; font-weight: 500; }
.opt {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  padding: 10px 12px;
  border: 1px solid var(--line-soft);
  border-radius: 7px;
  margin-bottom: 8px;
  cursor: pointer;
  transition: border-color 0.12s ease, background 0.12s ease;
}
.opt:hover { border-color: var(--brass); background: #fbf6ea; }
.opt input { margin-top: 3px; accent-color: var(--brass-dark); }
.opt.selected { border-color: var(--brass-dark); background: var(--brass-tint); }
.opt.review-correct { border-color: var(--good); background: var(--good-tint); }
.opt.review-wrong { border-color: var(--bad); background: var(--bad-tint); }

.submit-bar {
  position: sticky;
  bottom: 0;
  background: linear-gradient(180deg, rgba(246,243,236,0) 0%, var(--paper) 30%);
  padding: 18px 0 6px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}
.submit-bar .hint { color: var(--ink-soft); font-size: 0.86rem; }

/* ---------- Results ---------- */
.score-panel {
  background: var(--navy-950);
  color: #fff;
  border-radius: var(--radius);
  padding: 30px 26px;
  text-align: center;
  margin-bottom: 24px;
}
.score-panel .big { font-family: "Lora", serif; font-size: 2.6rem; color: var(--brass); }
.score-panel .sub { color: #c3ccdd; margin-top: 4px; }

.review-item {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-left: 4px solid var(--bad);
  border-radius: var(--radius);
  padding: 18px 20px;
  margin-bottom: 12px;
}
.review-item.correct { border-left-color: var(--good); }
.review-item .q-text { font-weight: 500; margin-bottom: 10px; }
.review-line { font-size: 0.92rem; margin: 3px 0; }
.review-line .label { color: var(--ink-soft); }
.review-line.right { color: var(--good); font-weight: 600; }
.review-line.wrong { color: var(--bad); font-weight: 600; }

/* ---------- Leaderboard ---------- */
.leaderboard {
  background: var(--paper-card);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  overflow: hidden;
}
.leaderboard table { width: 100%; border-collapse: collapse; }
.leaderboard th {
  text-align: left;
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--ink-soft);
  background: var(--line-soft);
  padding: 10px 16px;
}
.leaderboard td {
  padding: 11px 16px;
  border-top: 1px solid var(--line-soft);
  font-size: 0.94rem;
}
.leaderboard tr.me { background: var(--brass-tint); }
.rank { font-family: "Lora", serif; color: var(--brass-dark); width: 2.5em; }
.leaderboard .empty { padding: 22px 16px; text-align: center; color: var(--ink-soft); }

/* ---------- Section heading with rule ---------- */
.section-head {
  display: flex;
  align-items: baseline;
  gap: 12px;
  margin: 34px 0 16px;
}
.section-head h2 { font-size: 1.15rem; white-space: nowrap; }
.section-head .rule { flex: 1; height: 1px; background: var(--line); }

.banner {
  border: 1px solid var(--line);
  background: var(--brass-tint);
  color: #4a3410;
  border-radius: 8px;
  padding: 12px 16px;
  font-size: 0.88rem;
  margin-bottom: 20px;
}

footer {
  text-align: center;
  color: var(--ink-faint);
  font-size: 0.82rem;
  padding: 30px 0 50px;
}

@media (max-width: 560px) {
  .quiz-card { flex-wrap: wrap; }
  .quiz-card .num { border-right: none; padding-right: 0; }
  .quiz-card .actions { width: 100%; }
  .quiz-card .actions .btn { flex: 1; }
}
